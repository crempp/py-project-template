# Use dev settings by default
#
# Defaulting to production settings will allow an improperly configured
# production app server to operate. We don't want that.
from project_dir.settings.local import *